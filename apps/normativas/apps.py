from django.apps import AppConfig


class NormativasConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.normativas'  # This should include 'apps.'
    verbose_name = 'Normativas y Seguridad'
